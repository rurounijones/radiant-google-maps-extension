%h1 Remove Snippet

%p 
  Are you sure you want to 
  %strong.warning permanently remove
  the following map?

%table.index#maps{:cellpadding=>0, :cellspacing=>0, :border=>0}
  %tbody
    %tr.node.level-1
      %td.map
        = image('map', :alt => "map-icon")
        %span.title= @google_map.name

- form_for [:admin, @google_map], :html => {:method => :delete} do
  %p.buttons
    %input.button{:type=>"submit", :value=>"Delete Map"}/
    or
    = link_to 'Cancel', admin_google_maps_url